---
name: feedback_dev_command
description: How to start the Outpost dev server
metadata:
  type: feedback
---

Run the dev server with `bun dev`. No need to look for other scripts or commands.

**Why:** User corrected me when I tried to discover the launch command via a script — they confirmed it's simply `bun dev`.

**How to apply:** Always use `bun dev` to start the Outpost development server. Don't run discovery scripts first.
